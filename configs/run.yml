groups:
  - name: terrain_features
    features: [dem_elev, slope]
    summary_statistics: [mean, std, q10, q90]
    buffer_sizes: [100, 300, 1000]
    output:
      kind: tabular
      dump_windows: true    # <- enable per-point window GeoTIFFs

project_crs: EPSG:3006
out_dir: out
