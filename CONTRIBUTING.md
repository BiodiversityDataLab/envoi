# Contributing

- Open an issue for scope changes.
- Add datasets/sources via `configs/catalog.yml` (see `docs/datasets.md`).
- Write tests for new reducers/adapters.
- Keep big data out of git; use `data/` for tiny samples only.
